{@
    vec = []

    def f(dep):
        if dep >= 3:
            return
        end
        vec.push(&dep)
        f(dep + 1)
    end

    f(0)

    for i = 0; i < len(vec); i += 1:
        v = vec[i]
        puts(*v)
    end
@}