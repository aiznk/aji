{@
	/* Bubble sort code.
	 * Written by Aji
	 */
	v = [3, 1, 2, 4, 5]

	for i = 0; i < len(v); i += 1:
		for j = i; j < len(v); j += 1:
			if v[i] > v[j]:
				// Swap
				tmp = v[i]
				v[i] = v[j]
				v[j] = tmp
				break
			end
		end
	end

	puts(v)
	// [1, 2, 3, 4, 5]
@}
