{@
    s = "This is a pen.
This is a pen.
This is a pen."

    // s = s.replace("\n", " ")
    words = const(s.split(" "))

    for i = 0; i < len(words); i += 1:
        word = words[i]
        word = word.capitalize()
        puts(word, len(word))
    end
@}
